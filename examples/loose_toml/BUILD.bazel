load("@ecosia_bazel_rules_nodejs_contrib//:defs.bzl", "toml_to_js")

toml_to_js(
    name = "loose_toml",
    srcs = [
        "another_config.toml",
        "config.toml",
    ],
    strict = False,
)
